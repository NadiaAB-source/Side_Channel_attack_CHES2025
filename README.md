# Side_Channel_attack_CHES2025
PyTorch framework for profiling side-channel attacks on AES, featuring Quadrant Scan–based preprocessing and convolutional-Transformer models, evaluated using Guessing Entropy (GE) and NTGE on CHES-style datasets.
